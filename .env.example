# Azure DevOps Configuration
# Copy this file to .env and fill in your values

# Your Azure DevOps organization name (the part after dev.azure.com/)
ADO_ORG=ph19dev

# Your project name (for work items and sprints)
ADO_PROJECT=cicd-sandbox

# Project for Pull Requests (defaults to ADO_PROJECT if not set)
# Use this if PRs are in a different project
ADO_PROJECT_PRS=cicd-sandbox
ADO_PROJECT_PRS=cicd-sandbox

# Personal Access Token
# Create one at: https://dev.azure.com/{org}/_usersSettings/tokens
# Required scopes: 
#   - Work Items (Read) - for dashboard
#   - Work Items (Read) - if using scaffold script
ADO_PAT=

# Optional: Team name (defaults to "{project} Team")
# ADO_TEAM=Your Team Name

# Your ADO user display name or email (for filtering "my" work items)
# This should match how your name appears in ADO's "Assigned To" field
ADO_USER=

# Backend port (optional, defaults to 3001)
PORT=3001
